import { NextRequest,NextResponse } from "next/server"; import { validDiagnosis } from "../../../lib/server";
export async function POST(req:NextRequest){try{const body=await req.json();if(!validDiagnosis(body?.diagnosis)||!body?.ability)return NextResponse.json({error:"Invalid share data"},{status:400});const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:"共有機能を有効にするにはSupabaseを設定してください。"},{status:503});const id=crypto.randomUUID(),d=body.diagnosis;const r=await fetch(`${url}/rest/v1/diagnoses`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:"return=minimal"},body:JSON.stringify({diagnosis_id:id,main_type:d.mainType,scores:d.scores,personality:null,ability:body.ability,ability_rating:body.ability.rating})});if(!r.ok)throw Error();return NextResponse.json({id})}catch{return NextResponse.json({error:"共有リンクを作成できませんでした。"},{status:500})}}
