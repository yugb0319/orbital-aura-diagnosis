import { Scores,TYPES } from "../lib/types";
export function Radar({scores}:{scores:Scores}){const c=150,r=108;const points=TYPES.map((t,i)=>{const a=-Math.PI/2+i*Math.PI/3,v=scores[t]/100;return `${c+Math.cos(a)*r*v},${c+Math.sin(a)*r*v}`}).join(" ");return <svg className="radar" viewBox="0 0 300 300" role="img" aria-label="6系統の適性チャート"><polygon points="150,42 244,96 244,204 150,258 56,204 56,96" fill="#121a36" stroke="#405080"/><polygon points={points} fill="#78dfff55" stroke="#55e6db" strokeWidth="3"/>{TYPES.map((t,i)=>{const a=-Math.PI/2+i*Math.PI/3;return <text key={t} x={c+Math.cos(a)*135} y={c+Math.sin(a)*135} fill="#cbd5f1" fontSize="12" textAnchor="middle">{t.replace("系","")}</text>})}</svg>}
